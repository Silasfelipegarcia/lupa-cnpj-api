services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: lupacnpj
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - lupacnpj_pg:/var/lib/postgresql/data

volumes:
  lupacnpj_pg:
